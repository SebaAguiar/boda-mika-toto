/* eslint-disable @typescript-eslint/no-explicit-any */

export class MusicService {
  // private readonly db = db;

  postMusicRecomendarion(body: any) {
    console.log(body);
    try {
    } catch (error) {
      console.log(error);
      throw error;
    }
  }
}
